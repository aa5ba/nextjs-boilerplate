# مشروع احتساب / محطة العمل

## 1. تعريف المشروع

هذا المستودع يحتوي مشروعًا مبنيًا بـ:

- Next.js `16.2.4`
- React `19.2.4`
- TypeScript
- Supabase
- Next.js App Router

منصة احتساب مستقلة منطقيًا عن محطة العمل، ومسارات `/finance/[branch]/` تخص محطة العمل. صفحة الدخول الموحدة هي `/login`، و`app/page.tsx` يحوّل إليها مباشرة. لوحة الدعم الفني لها جلسة وحماية منفصلة عن جلسة موظف الفرع.

هذا الإصدار من Next.js ليس بالضرورة مطابقًا للمعرفة العامة السابقة. قبل تعديل أي جزء متعلق بـ Next.js يجب قراءة التوثيق المناسب الموجود داخل:

`node_modules/next/dist/docs/`

## 2. قواعد تنفيذ المهام

- اقرأ `AGENTS.md` و`PROJECT_REFERENCE.md` قبل أي مهمة.
- افحص الملفات المرتبطة كاملة قبل التعديل.
- لا تعدّل ملفات خارج نطاق المهمة.
- لا تنفذ إعادة هيكلة واسعة دون طلب صريح.
- لا تضف Dependencies دون موافقة صريحة.
- لا تنفذ `git push` أو deploy دون طلب صريح.
- لا تقرأ أو تطبع الأسرار أو ملفات `.env`.
- اعرض خطة قصيرة والملفات المتوقع تعديلها قبل التنفيذ.
- بعد التعديل اعرض الملفات المتغيرة وسبب كل تغيير.
- اذكر عدد الأسطر قبل وبعد كل ملف إن أمكن.
- اشرح سبب الزيادة أو النقصان.
- لا تحذف أكوادًا قائمة دون توضيح السبب.
- لا تعتبر المهمة مكتملة حتى تنجح الفحوصات المطلوبة، إلا إذا ثبت أن الخطأ سابق وغير مرتبط بالتعديل.

## طريقة العمل الإلزامية مع Codex

- تعامل مع كل طلب من المستخدم كمهمة واحدة محددة.
- قبل أي تعديل، اقرأ الملفات المرتبطة بالمهمة كاملة وحدد:
  - سبب المشكلة أو التغيير.
  - الملفات المتوقع تعديلها.
  - خطة قصيرة للتنفيذ.
  - المخاطر أو النقاط التي تحتاج تحققًا.
- لا تعدّل الملفات إذا كان طلب المستخدم مراجعة أو تخطيطًا فقط.
- إذا طلب المستخدم خطة قبل التنفيذ، توقف بعد عرض الخطة وانتظر موافقة صريحة.
- لا توسّع نطاق المهمة بسبب مشاكل جانبية تكتشفها أثناء العمل.
- سجّل المشاكل الإضافية كملاحظات فقط، ولا تصلحها دون طلب منفصل.
- بعد تنفيذ المهمة اعرض Diff، والملفات التي تغيرت، والفحوصات التي أجريتها، ثم توقف.
- لا تبدأ مهمة تالية تلقائيًا.
- لا تنفّذ stage أو commit أو push أو deploy دون موافقة صريحة.
- لا تعتبر تقريرك النصي بديلًا عن مراجعة Diff الفعلي.
- ميّز دائمًا بين:
  - مؤكّد من الملفات.
  - مستنتج من الاستخدام.
  - قرار مشروع معتمد.
  - يحتاج فحص Supabase.
  - اقتراح غير منفذ.
- إذا تعارضت وثائق المشروع مع الكود الفعلي، فالملفات الفعلية هي المصدر الحالي للحقيقة. سجّل التعارض قبل أي تعديل.

## الأوامر الحساسة والممنوعة دون موافقة

- لا تشغّل أوامر تغيّر قاعدة البيانات دون موافقة صريحة ومنفصلة.
- لا تنفّذ SQL مباشرًا على الإنتاج.
- لا تشغّل `supabase db push` أو `supabase db reset --linked` أو `supabase migration repair` دون طلب صريح ومراجعة أثر الأمر.
- لا تشغّل أوامر حذف أو إعادة إنشاء schema أو بيانات دون طلب صريح ومراجعة أثر الأمر.
- أي سحب schema أو توليد types يجب أن يكون مهمة مستقلة، مع توضيح الأمر وأثره قبل التنفيذ.
- لا تسحب أو تحفظ بيانات العملاء أو العقود أو السداد أو أي data dumps داخل المستودع.
- لا تحفظ access tokens أو db passwords أو service role keys في Git.
- لا تستخدم أوامر Git مدمّرة دون موافقة صريحة، بما في ذلك:
  - `git reset --hard`
  - `git clean -fd`
  - `git checkout --`
  - `git restore` على تغييرات المستخدم دون مراجعة.
  - `git push --force`
- لا تعدّل `package-lock.json` ولا تثبّت dependencies إلا إذا كانت المهمة مرتبطة بذلك وموافقًا عليها صراحة.

## 3. مصدر الثقة في الجلسة

جلسة موظف الفرع الحديثة تعتمد Cookie موقعة:

`finance_branch_session`

جلسة الدعم تعتمد:

`admin_support_session`

جلسة انتحال الفرع من الدعم تستخدم:

`admin_support_impersonation`

Cookie الخادمية هي مصدر الثقة في هوية المستخدم والفرع والدور والصلاحيات. `localStorage` طبقة توافق وعرض للصفحات القديمة فقط، ويمنع الاعتماد عليه وحده لحماية عملية حساسة.

استخدم `requireFinanceBranchSession` أو الحماية الخادمية المعتمدة في API Routes الحساسة. لا تمسح الجلسة بسبب فشل تحميل بيانات عادي، ولا تسجّل خروج المستخدم إلا عند ثبوت بطلان الجلسة.

تسجيل الخروج الصحيح لموظف الفرع يمر عبر:

`/finance/api/branch-logout`

استخدم `credentials: "include"` عند الحاجة لإرسال الكوكيز، وبعد نجاح تسجيل الخروج امسح كل مفاتيح جلسة المحطة من `localStorage`، وليس بعض المفاتيح فقط.

بعد تسجيل الخروج يستخدم:

`router.replace("/login")`

لا تستخدم full reload للخروج إلا إذا وُثّقت ضرورة ذلك.

يمنع استخدام مسار غير موجود مثل:

`/api/finance/login`

مفاتيح `localStorage` المؤكدة من الملفات تشمل:

- `finance_user`
- `finance_branch_user`
- `finance_user_id`
- `finance_user_name`
- `finance_username`
- `finance_role`
- `finance_branch_id`
- `finance_branch_slug`
- `finance_branch_name`
- `finance_organization_name`
- `finance_permissions`
- `finance_manageable_permissions`
- `finance_investor_id`
- `finance_is_active`
- `finance_last_login_at`
- `finance_phone`
- `finance_theme_key`
- `finance_session_version`
- `finance_permissions_version`
- `finance_session_expires_at`
- `finance_last_activity_at`
- `finance_last_remote_check_at`
- `finance_return_to`

## 4. عزل الفروع

- كل قراءة أو إنشاء أو تحديث أو حذف لبيانات تابعة لفرع يجب أن يفرض `branch_id`.
- يجب مطابقة الفرع في الرابط مع الفرع الموجود في الجلسة الخادمية.
- في `/finance/[branch]/`، الحصول على `branch_id` والتحقق من مطابقته للجلسة إلزامي لكل عملية على بيانات الفرع.
- لا تثق بأي `branch_id` قادم من العميل.
- العمليات الحساسة يجب أن تشتق أو تتحقق من الفرع في الخادم.
- أي تحديث أو حذف مباشر لسجل تابع لفرع يجب أن يفلتر بمعرّف السجل و`branch_id` مع حماية RLS أو API أو RPC مناسبة.
- انتبه خصوصًا إلى `finance_notes`.
- لا يجوز ظهور أو تعديل بيانات فرع داخل فرع آخر.
- لا تعتمد على إخفاء الأزرار لفرض الصلاحيات.
- العمليات الحساسة يجب أن تُحمى في API خادمية أو داخل RPC تتحقق فعليًا من الجلسة والفرع والصلاحية.

## 5. Supabase والأسرار

- `lib/supabaseAdmin.ts` خادمي فقط ويجب أن يبقى مستخدمًا مع `server-only`.
- يمنع استخدام Service Role داخل Client Component.
- يمنع وضع Service Role في متغير يبدأ بـ `NEXT_PUBLIC_`.
- عميل المتصفح يستخدم مفتاح anon فقط.
- لا تطبع بيانات الجلسة أو الرموز أو المفاتيح في السجلات.
- لا تنفذ تعديلًا مباشرًا على بيانات الإنتاج.
- أي Migration يجب أن تكون واضحة وقابلة للمراجعة والتراجع.

## 6. قواعد RPC والعمليات الحساسة

أي RPC حساسة يجب مراجعة الآتي قبل الاعتماد عليها:

- تعريف SQL.
- نوع `SECURITY DEFINER` أو `SECURITY INVOKER`.
- طريقة تحديد المستخدم الحالي.
- التحقق من `branch_id`.
- التحقق من الصلاحية.
- منع التلاعب بمعرف الفرع القادم من الواجهة.
- ذرية العملية.
- تسجيل النشاط.
- منع القيم السالبة.
- منع تجاوز السداد.
- التعامل مع الأخطاء.
- القيود على الأدوار.
- سياسات RLS المرتبطة.

استدعاء RPC من Client Component لا يعد آمنًا تلقائيًا.

- لا تضف أو تغيّر أسماء RPC دون إعادة فحص الاستخدام الفعلي.
- الأسماء المتعارضة من مراجعات سابقة لا تُعد مؤكدة إلا إذا وُجد مسار استدعاء فعلي لها.
- لا تستنتج أمان RPC من الاسم أو من أسلوب الاستدعاء فقط.

### RPC تغيّر البيانات أو الحالة

- `record_payment_atomic`
- `cancel_payment_atomic`
- `close_contract_atomic`
- `reopen_contract_atomic`
- `declare_contract_default_atomic`
- `update_finance_contract_atomic`
- `update_customer_atomic`
- `delete_customer_atomic`
- `block_finance_customer`
- `unblock_finance_customer`
- `process_expense_invoice_atomic`
- `delete_expense_invoice_atomic`
- `update_expense_invoice_atomic`
- `update_own_finance_settings_atomic`
- `change_own_finance_password_atomic`
- `self_disable_finance_account_atomic`
- `create_finance_user_server_atomic`
- `create_finance_investor_server_atomic`
- `toggle_finance_user_server_atomic`
- `update_finance_user_server_atomic`
- `delete_finance_user_server_atomic`
- `create_promissory_note_secure_atomic`

### RPC قراءة حساسة

- `get_own_finance_settings_secure`

ضع `get_own_finance_settings_secure` ضمن القراءة الحساسة ما لم يثبت الاستخدام الفعلي أنه يغيّر بيانات.

## مراجعة قاعدة البيانات

- تعريفات الجداول وRPC وRLS وTriggers وGrants لا تُعد مؤكدة بالكامل حتى تتوفر Schema أو Migrations الفعلية.
- قبل أي إصلاح متعلق بقاعدة البيانات، تحقق من SQL الفعلي أو سجّل أن النقطة تحتاج فحص Supabase.
- لا تنشئ migration اعتمادًا على أسماء أعمدة أو constraints مخمّنة.

## 7. العمليات الذرية

أي عملية تعدل أكثر من جدول أو عدة سجلات مترابطة يجب أن تكون ذرية. أمثلة ذلك:

- العقد + العميل.
- العقد + السند.
- العقد + المخزون.
- الدفعة + العقد.
- إلغاء الدفعة + إعادة الحساب.
- الإغلاق + إعادة الحساب.
- إضافة كمية + حركة المخزون.
- تغيير منتج العقد + إرجاع وخصم المخزون.
- المصروف + سجل النشاط.
- إنشاء مستخدم + صلاحياته.
- إنشاء سند + سجل التغييرات.

يجب أن تنجح العملية كاملة أو تتراجع كاملة. لا تستبدل RPC ذرية بسلسلة استعلامات متفرقة من Client Component.

## 8. العقود

أعمدة ومسميات مستخدمة فعليًا في ملفات المشروع:

- `id`
- `branch_id`
- `contract_number`
- `customer_id`
- `finance_type`
- `investor_id`
- `investor_name`
- `product_id`
- `product_name`
- `product_quantity`
- `print_party_type`
- `print_party_name`
- `print_party_identifier`
- `debt_amount`
- `payment_amount`
- `installment_amount`
- `payment_type`
- `contract_date_hijri`
- `contract_date_gregorian`
- `payment_due_date`
- `guarantor_name`
- `guarantor_customer_id`
- `legal_city`
- `judicial_amount`
- `notes`
- `contract_status`
- `paid_amount`
- `remaining_amount`
- `closed_at`
- `default_declared_at`
- `default_expires_at`

لا تضف عمودًا على أنه مؤكد إلا بعد العثور على استخدام فعلي له في الملفات أو تعريف SQL.

## الطلب الجديد وإنشاء العقود

- نوع التمويل في الطلب الجديد اختياري.
- إذا لم يُحدد نوع التمويل، أرسل `null` ولا تجبر واجهة المستخدم قيمة غير موجودة.
- عند وجود تطابق على `(branch_id, national_id)`، استخدم العميل الموجود وحدث بياناته حسب قواعد العملية، ولا تنشئ عميلًا مكررًا.
- لا تتعامل مع كل خطأ PostgreSQL `23505` على أنه تعارض رقم عقد؛ افحص constraint أو details.
- إنشاء الطلب الجديد والعقد وسند لأمر والمخزون وحركة المخزون وسجل النشاط يجب أن يبقى ذريًا.
- لا تقسّم العملية الذرية إلى استعلامات عميل مستقلة.
- توليد رقم العقد أو رقم السند يجب أن يستخدم آلية معتمدة من قاعدة البيانات ويتجنب التعارضات.

## 9. السندات لأمر

يوجد فرق إلزامي بين السند الحر والسند المرتبط بعقد.

القواعد المعتمدة:

- العقد هو مصدر الحقيقة للسند المرتبط.
- لا تسمح لمدخلات الواجهة بتجاوز مبلغ العقد.
- بيانات المدين في السند المرتبط تؤخذ من عميل العقد.
- بيانات المستفيد تؤخذ من الطرف المعتمد في العقد.
- الكفيل الموروث يؤخذ من العقد.
- وراثة كفيل العقد لا تتطلب صلاحية تعديل الكفيل يدويًا.
- صلاحية إدارة الكفيل مطلوبة فقط عند الإضافة أو التعديل اليدوي.
- تاريخ استحقاق السند المرتبط هو `payment_due_date` الخاص بالعقد.
- لا يستخدم نص «عند الطلب» إذا كان العقد يحتوي على تاريخ استحقاق.
- المدينة اختيارية في المسارات الجديدة.
- لا تسمح للواجهة بتغيير البيانات الموروثة من العقد.
- إنشاء السند وسجل التغيير يجب أن يكونا ذريين.
- عند توليد رقم السند يجب تجنب غموض أسماء الأعمدة باستخدام alias واضح.

RPC المستخدمة فعليًا حاليًا لإنشاء السند هي:

`create_promissory_note_secure_atomic`

لا تستخدم أسماء RPC قديمة على أنها فعالة إذا لم تجدها في المشروع.

## التحقق وتدخل الدعم الفني

- صفحة الدعم لا تعرض كل العقود تلقائيًا عند الفتح إذا كان المسار معتمدًا على البحث.
- التحقق أو البحث عن العقد يكون برقم العقد أو رقم هوية العميل حسب المسار المعتمد.
- صفحة الفرع أو التحقق الخاصة بموظفي الفرع يجب ألا تعرض مؤشرًا أو ملاحظة أو سجلًا يوضح أن الدعم عدّل التحقق أو النتيجة أو الحالة يدويًا.
- إخفاء تدخل الدعم من واجهة الفرع لا يعني حذف سجل التدقيق الداخلي للدعم.
- سجل التدقيق الداخلي يبقى محميًا ومخفيًا عن موظفي الفرع.
- تغييرات الدعم اليدوية يجب أن تمر عبر API أو RPC محمي بصلاحية مناسبة.

## 10. السداد

- السداد يجب أن يعيد حساب `paid_amount` و`remaining_amount` و`contract_status`.
- إلغاء الدفعة يعيد الحساب من جديد.
- الإغلاق وإعادة التنشيط يجب أن يكونا ذريين.
- لا تسمح بمبلغ سالب.
- لا تسمح بالتجاوز إلا وفق قرار صريح ومعامل مخصص.
- يجب تسجيل عمليات السداد والإلغاء والإغلاق وإعادة التنشيط في سجل النشاط.
- يجب فرض `branch_id` والتحقق من الجلسة والصلاحية.
- لا تستخدم بيانات الواجهة كمصدر ثقة وحيد.

## 11. التأخر والتعثر

### متأخر

يبدأ العقد متأخرًا بعد انتهاء يوم الاستحقاق ودخول اليوم التالي، بشرط:

- وجود مبلغ متبقٍ.
- ألا يكون العقد مسددًا.
- ألا يكون مغلقًا.
- ألا يكون ملغيًا.

### مؤهل لإعلان التعثر

لا يصبح العقد مؤهلًا إلا بعد مرور 7 أيام كاملة على تاريخ الاستحقاق، مع وجود مبلغ متبقٍ.

### متعثر معلن

لا يصبح العقد متعثرًا تلقائيًا. يصبح متعثرًا فقط بعد نجاح عملية:

`declare_contract_default_atomic`

يمنع خلط الحالات الثلاث في الحسابات أو واجهة المستخدم.

## 12. المخزون

- كل إضافة أو خصم أو إرجاع كمية يجب أن يسجل حركة في `finance_inventory_movements`.
- تعديل الرصيد وإضافة الحركة يجب أن يكونا في عملية ذرية واحدة.
- تغيير المنتج أو المستثمر أو الكمية في العقد يجب أن يعالج المخزون ذريًا.
- لا تغيّر تصميم صفحة المخزون عند إصلاح منطق داخلي.
- بيانات المستثمرين والمنتجات والمخزون معزولة حسب الفرع.
- لا تعتمد على `localStorage` لفرض صلاحية تعديل المخزون.

## 13. المستثمرون

صلاحيات المستثمرين المؤكدة من الملفات:

- `add_investor`
- `edit_investor`
- `toggle_investor`
- `investor_data`
- `investor_contracts`

المدير قد يتجاوز بعض الصلاحيات بحسب `MANAGER_ROLES`، لكن يجب ألا يعتمد الأمان على الواجهة فقط.

## 14. الموظفون والصلاحيات

أدوار الفروع المؤكدة من الملفات تشمل:

- `main_admin`
- `branch_manager`
- `admin`
- `manager`
- `مدير رئيسي`
- `مدير فرع`
- `مدير`
- `موظف`
- `مستثمر`

أدوار الدعم الفني المؤكدة:

- `super_admin`
- `support`
- `viewer`

صلاحيات الفروع وصلاحيات لوحة الدعم موثقة في `PROJECT_REFERENCE.md`. لا تدمج الأدوار الإنجليزية والعربية على أنها متطابقة إلا إذا أثبت الكود ذلك.

## 15. التصميم

القواعد المعتمدة:

- الخط: `var(--font-almarai), sans-serif`
- الخلفية: `/backgrounds/v13-finance-bg-1.png`
- الهيدر بتدرج أزرق غامق وأزرق وسماوي.
- دوائر CSS خفيفة داخل الهيدر.
- لا توجد صورة داخلية في الهيدر.
- لا يوجد قفل أو رمز أمان في الهيدر.
- لا يوجد وصف أسفل عنوان الصفحة.
- زر «محطة العمل الرئيسية» أخضر متدرج.
- زر الرجوع أخضر متدرج.
- زر الرجوع يستخدم `router.back()`.
- التنقل الداخلي يستخدم `router.push` أو `router.replace` أو `router.back`.
- يمنع استخدام `window.location.href` أو `window.location.reload` للتنقل المعتاد.
- حافظ على responsive للجوال والتابلت والكمبيوتر.
- لا تغيّر تصميم صفحة سليمة عند إصلاح مشكلة منطقية.
- جميع عناوين الصفحات تستخدم Almarai ما لم يطلب المستخدم غير ذلك.

## 16. العربية والأرقام

- المشروع RTL ويدعم النصوص العربية.
- استخدم helper موحد من `lib/numberUtils.ts` عند الإمكان.
- حوّل الأرقام العربية `٠١٢٣٤٥٦٧٨٩` والفارسية `۰۱۲۳۴۵۶۷۸۹` إلى `0123456789`.
- يجب أن يظهر التحويل داخل الحقل فور الإدخال.
- التخزين في قاعدة البيانات يكون بالأرقام الإنجليزية.
- لا تكرر منطق التحويل داخل الصفحات إذا كان helper موحدًا متاحًا.
- طبّق ذلك على الهوية والجوال ورقم العقد والمبالغ والكميات والتواريخ الرقمية وكلمات المرور الرقمية وأي مدخل رقمي.

## 17. الأداء

- استخدم server-side pagination للقوائم الكبيرة.
- استخدم server-side search.
- لا تحمل جميع سجلات العملاء أو العقود أو الدفعات أو الحركات دفعة واحدة.
- استخدم `range` أو pagination حقيقية.
- استخدم cancelled flag أو `AbortController` عند وجود احتمال race condition.
- لا تستخدم reload كاملًا لإعادة تحميل البيانات.
- لا تنفذ استعلامات متكررة دون حاجة.

## 18. الفحوصات بعد التعديل

بعد أي مهمة برمجية شغّل، عند توفر Dependencies:

- `npm run lint`
- `npm run build`
- الاختبارات المرتبطة إن وجدت

راجع أيضًا توازن الأقواس، صحة TypeScript، imports، المتغيرات غير المستخدمة، الدوال والأسماء المحذوفة، المسارات، فلترة `branch_id`، الصلاحيات، عدم تسرب الأسرار، وعدم تعديل ملفات خارج نطاق المهمة.

## 19. تقرير التسليم

في نهاية كل مهمة يجب عرض:

- سبب المشكلة.
- الملفات التي تم تعديلها.
- التعديل داخل كل ملف.
- عدد الأسطر قبل وبعد.
- سبب الزيادة أو النقصان.
- نتائج lint وbuild والاختبارات.
- أي خطأ سابق غير مرتبط بالتعديل.
- المخاطر المتبقية.
- المراجعة اليدوية المطلوبة.
- تأكيد عدم تعديل ملفات غير مرتبطة.
